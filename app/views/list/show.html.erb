<p>
  <strong>My List Title:</strong>
 <p> <%= @list.title %></p>

<h2>Items:</h2>
<% @list.items.each do |i| %>
  <p>
    <%= i.item %>
  </p>


     | <%= link_to 'Change item', [:edit, @list, i] %> |
      <%= link_to 'Destroy', [@list, i],
          method: :delete, data: { confirm: 'Are you sure?'}
          %>

<% end %>

<h2>Add an Item</h2>
<%= render "items/form" %>


 <p><%= link_to 'Back', list_index_path %>
     | <%= link_to 'Edit', edit_list_path(@list) %> |
      <%= link_to 'Create a New List', new_list_path(@list) %> |
      <%= link_to 'Delete', @list, method: :delete, data: { confirm: 'Are you sure?' } %> |

</p>



